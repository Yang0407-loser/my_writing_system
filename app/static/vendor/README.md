# Vendored Frontend Dependencies

- `vue.esm-browser.prod.js`: Vue 3.5.13 production ESM browser build.
- Source: official Vue distribution package.
- License: MIT, see `vue.esm-browser.prod.js` header and the Vue repository.

The application imports this local file so the writing workspace can start
without depending on a public CDN at runtime.
