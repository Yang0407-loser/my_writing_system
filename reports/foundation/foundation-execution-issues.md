# Foundation Execution Issue Ledger

This ledger records issues discovered during the first implementation pass. In
accordance with the approved execution policy, non-blocking issues remain open
until Tasks 3-20 have received their initial implementation, then enter the
central remediation pass. Gate criteria are not relaxed by deferral.

| ID | First observed | Impact | Evidence | Status |
| --- | --- | --- | --- | --- |
| FND-001 | Task 2 full-suite diagnostic | The default full suite could not finish collection because a historical experiment module was absent. | Restored `wr310_reviewer_real_task_side_by_side.py`; final suite collects and completes. | Resolved |
| FND-002 | Task 2 full-suite diagnostic | 124 legacy tests initially failed because the isolated worktree did not inherit ignored, hash-locked historical runtime artifacts. | Synced the local test-only artifacts from the frozen baseline workspace without committing secrets; final Python 3.11 suite: `1893 passed, 12 skipped, 0 failed`. | Resolved; 2 skips intentionally require a private secret-bearing fixture |
| FND-003 | Foundation planning | The mandatory PostgreSQL integration gate has not been proven on this workstation. | Final Canonical integration: `14 passed, 10 skipped`; all 10 skips say `TEST_CANONICAL_DATABASE_URL is required`. | Open external blocker; `internal_required` remains prohibited |
| FND-004 | Foundation planning | Runtime parity risk: the initial environment used Python 3.14 while production targets Python 3.11. | Created `.venv311` with CPython 3.11.5 from the locked dependency graph; full gate is green (`1893 passed`). | Resolved |
| FND-005 | Task 3 Red test | A resume-path exception could be masked by an `UnboundLocalError`. | Initialized timing state before both new/resume branches and preserved sanitized failure checkpoints; directed and full regressions pass. | Resolved |
| FND-006 | Task 3 directed gate | Windows kept temporary `tasks.db` handles open during pytest cleanup. | Added context lifecycle, initialization-failure cleanup, weak instance tracking and deterministic close at every production caller; repeated-open regression and full suite pass. | Resolved |
| FND-007 | Task 5 snapshot generation | Duplicate `/tasks` operation IDs made generated-client naming ambiguous. | Removed the duplicate route; frozen OpenAPI contract and final suite pass without the duplicate-operation warning. | Resolved |
| FND-008 | Task 6 Docker smoke / Tasks 10 and 14 PostgreSQL gates | Docker image build and real PostgreSQL schema/concurrency/crash/replay gates cannot run. | Final retry with an explicit local test password reaches Docker and fails on missing `npipe:////./pipe/docker_engine`; the daemon is not running. | Open external blocker; start Docker and rerun the PostgreSQL Gate |
| FND-009 | Task 15 verification | Ruff was absent from the locked development environment. | Added Ruff to the dev lock, froze the project rule set, and passed the Python 3.11 Foundation static scope. | Resolved |
| FND-010 | Task 17 combined regression | Alembic disabled unrelated existing loggers, breaking later log assertions. | Set `disable_existing_loggers=False`; combined and full regressions pass. | Resolved |
| FND-011 | Final Python 3.11 Gate | Equivalent `typing.Optional[Callable]` and PEP 604 annotations produced different signature strings on Python 3.11/3.14. | Canonicalized annotation spellings in contract snapshots; the frozen contract passes on both Python versions and the public signature is unchanged. | Resolved |
| FND-012 | Independent Foundation review | The first review found two Critical gaps: the real Writer loop did not call Canonical runtime, and handover/validation observed pre-length-adjustment prose. | Added a Coordinator-owned Canonical Writer bridge, per-subsection single-path routing, post-commit compatibility projectors and regression proving no legacy subsection commit; moved adjustment before evidence extraction/validation. | Resolved; directed 58-test group and full suite pass |
| FND-013 | Independent Foundation review | The review found three Important gaps: `internal_required` accepted non-PostgreSQL production URLs, TaskStore retained strong instances, and Commit Service did not bind Candidate fields to the locked subsection row. | Production `internal_required` now requires PostgreSQL; TaskStore uses `WeakSet` plus explicit close; Commit Service validates document, ordinal and legacy coordinates under lock. | Resolved |
| FND-014 | Final full-suite remediation | Branch-aware Writer control flow invalidated brittle source-string tests; isolated tests also lacked local read-only historical fixtures and one deterministic capacity report had drifted. | Updated tests to assert semantic call ordering across both branches, copied optional private fixtures only into the per-session temp runtime, and synchronized deterministic capacity evidence. | Resolved; final suite `1893 passed, 12 skipped` |
| FND-015 | Final pre-commit static gate | Expanding the Foundation Ruff scope to the touched configuration module exposed three pre-existing E402 violations from late standard-library imports. | Used the failing Ruff command as the regression check, moved `logging`, `sys`, and `ContextVar` imports to the module header without behavior changes, then reran the identical scope. | Resolved; Ruff reports `All checks passed!` |
